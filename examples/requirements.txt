# Deps for examples/cloudchef_scooping_runner.py (proprioceptive harness template)
# and examples/fern_action_contract.py (action contract). Both are self-contained
# and need NO internal repo — just these. You bring your own policy + its deps
# (e.g. torch) separately.
requests>=2.31
numpy>=1.24
pillow>=10.0
